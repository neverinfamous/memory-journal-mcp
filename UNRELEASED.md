# Unreleased Changes

## [Unreleased](https://github.com/neverinfamous/memory-journal-mcp/compare/v7.3.0...HEAD)

### Added
- Phase 20 Verification: Successfully test-driven verified `Core CRUD` processes strictly via `Code Mode` (`mj.core.*`, `mj.admin.*`), achieving 100% test pass on entry creation, structured errors, parameter mapping, and relationship scoping without any required remediation.
- Phase 27 Verification: Successfully test-driven verified `tag management`, `export`, and `backup/restore` processes strictly via `Code Mode`, achieving 100% test pass on structured returns, path traversal blocking, and database constraints.
- Phase 17 Verification: Successfully test-driven verified `Code Mode` API discoverability (`mj.help()`, per-group help, method aliases, and positional arguments), confirming perfect encapsulation and read-only constraints.
- Phase 29 Verification: Successfully verified structured error handling across all 10 `mj.*` API groups via the Code Mode sandbox. Validated `{}` empty params, type mismatches, and domain boundary errors, confirming all endpoints reliably return structured `{success: false}` without crashing the sandbox or leaking raw MCP exceptions.
- Phase 25 Verification: Successfully verified all 16 GitHub API endpoints (`mj.github.*`) strictly via `Code Mode`, ensuring proper handling of Kanban operations, milestone CRUD, error handling, insights fetching, and repo lookup. Zero unhandled exceptions or memory leaks recorded.
- Phase 26 Verification: Successfully verified IO & Interoperability features via Code Mode (`mj.io.*`), including legacy `exportEntries` JSON/Markdown, alongside structured error handling and path traversal protection for `exportMarkdown` and `importMarkdown`. All tests successfully passed without remediation required.
- Phase 23 Verification: Successfully test-driven verified Cross-Group Orchestration via Code Mode (`mj.execute_code`), ensuring seamless interoperability between `analytics`, `core`, `github`, `search`, and `admin` API groups. Validated token-efficient execution of multi-step pipelines (Create → Index → Search) entirely within the sandbox without remediation required.
- Phase 30 Verification: Successfully verified payload optimization techniques via Code Mode. Tested Kanban item throttling (`summary_only`, `item_limit`), issue body truncation (`truncate_body`, `include_comments`), `MAX_QUERY_LIMIT` implementation across multiple services, and the hard 100KB Result Size Cap. All optimizations function correctly and return predictable structured errors or truncated outputs without failure.
- Phase 18 Verification: Successfully test-driven verified `Code Mode` Readonly Mode. Validated that read operations and `mj.help()` succeed, write operations reliably throw "Operation X is not found in group" due to dynamic filtering, and default mode strictly allows writes without error.
