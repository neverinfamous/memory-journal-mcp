# Unreleased Changes

## [Unreleased](https://github.com/neverinfamous/memory-journal-mcp/compare/v7.4.0...HEAD)

### Added
- **Skills**: Integrated the `github-copilot-cli` agent skill to provide interactive terminal-native adversarial code reviews.
- **Workflows**: Added the `copilot-audit` GitHub Commander workflow to establish a pre-push review loop evaluating uncommitted local diffs via Copilot.
- **Hush Protocol**: Added `pass_team_flag` and `resolve_team_flag` tools for machine-actionable team communication via structured `flag` entries.
- **Hush Protocol**: Added `memory://flags` resource surfacing active (unresolved) flags as a dashboard.
- **Hush Protocol**: Added `memory://flags/vocabulary` resource exposing the server-wide flag vocabulary.
- **Hush Protocol**: Added configurable flag vocabulary via `--flag-vocabulary` CLI flag and `FLAG_VOCABULARY` env var (defaults: blocker, needs_review, help_requested, fyi).
- **Briefing**: Integrated active flags summary into the briefing payload and user message table.
- **Briefing**: Added `localTime` field for chronological grounding in the briefing JSON payload.
- **Types**: Added `'flag'` to the `EntryType` union and `ENTRY_TYPES` constant.
- **Icons**: Added `ICON_FLAG` (alert triangle) for flag-related resources.

### Fixed
- **Types**: Added `importanceScore` formally to `JournalEntry` interface for stronger bounds ranking sorting.
- **Search**: Fixed early `.slice()` truncation executing before semantic importance `.sort()`, dropping high-relevance low-semantic edge cases.
- **Analytics**: Fixed SQLite `%Y-Q` grouping calculation bug by injecting explicit month-derivation syntax.
- **Constraints**: Enforced `MAX_QUERY_LIMIT` cap (500) against over-fetching from tag-broadened Team Searches instead of relying on a 1000 literal.
- **Consistency**: Hardened `teamCollaborationResource` returning `{ success: true, matrix: <object> }` rigidly. 
- **Briefings**: Rebranded the ambiguous `Matrix Density` insight label to `Relationship density`.
- **Hush Protocol**: Fixed SQLite parameter mapping bug where `autoContext` JSON strings were erroneously being cast to boolean integers (`1`/`0`) in `crud.ts`.

### Verification
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 2 GitHub Integration bounds (16 tools). Validated read-only context lookups, structured error architectures for non-existent entities, intelligent fallback default properties (`DEFAULT_PROJECT_NUMBER`), Kanban board mutations, multi-part issue/milestone lifecycles, and Copilot evaluations natively without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 5 Admin & Backup bounds (`merge_tags`, `list_tags`, `backup_journal`, `restore_backup`, `cleanup_backups`), confirming accurate tag consolidation, auto-timestamped backup creation, valid backup restoration with custom output fields, and absolute refusal of path-traversal without raw exception leaks.
- **Code Mode Validation**: Certified 100% test-pass rate across all Phase 27 tag management (`mergeTags`, `listTags`) and backup/restore (`backupJournal`, `restoreBackup`, `cleanupBackups`) functional bounds without remediation.
- **Code Mode Validation**: Certified 100% test-pass rate across all Phase 17 API Discoverability functional bounds (`mj.help()`, group help, aliases, positional arguments) without remediation.
- **Code Mode Validation**: Certified 100% test-pass rate across all Phase 20 Core CRUD functional bounds (`createEntry`, `getEntryById`, `updateEntry`, `deleteEntry`, etc.) including correct bridging of all metadata parameters and structural validation schemas without remediation.
- **Code Mode Validation**: Certified 100% test-pass rate across all Phase 29 Error Matrix and Zod Sweeps, validating that all 10 API groups (including the new Hush Protocol flags) return structured `{ success: false }` for empty parameters, type mismatches, domain bounds, and security violations without crashing the sandbox.
- **Code Mode Validation**: Certified 100% test-pass rate across all Phase 25 GitHub integrations (16 tools). Validated read-only lookups, structured error paths, Kanban operations, issue and milestone lifecycles, and Copilot reviews mapped to Code Mode implementations without regressions.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 26 IO & Interoperability functional bounds (`exportEntries`, `exportMarkdown`, `importMarkdown`), validating raw JSON/Markdown generation, filesystem-bound orchestration mapping, and safe path traversal halts (returning structured `{ success: false }`) without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 23 Cross-Group Orchestration bounds, proving robust pipeline execution (journal health dashboards, GitHub-journal coverage mappings, tag analyses, relationship graph summaries, create→index→search flows) sequentially entirely within sandbox contexts without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 24 Relationships & Visualization bounds (`linkEntries`, `visualizeRelationships`), verifying all five relationship types, validation mapping for duplicates/reversals/nonexistent targets, parameter reflection (descriptions), and topological graphing with Mermaid outputs without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 18 Readonly Mode bounds (`mj_execute_code` with `readonly: true` vs `readonly: false`), confirming write operations are strictly blocked while read and default mode operations execute successfully without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 16 Sandbox Basics bounds (`mj_execute_code`), confirming simple expression resolution, async/await bindings, robust global built-in injections, `metrics` timing propagation, and custom test timeouts alongside infinite loop truncation protection without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 21 Search & Semantics bounds, including FTS5 behaviors (phrase/prefix/boolean/filters), timestamp/importance bounds sorting, cross-database searching, analytics integration, date range edge cases, and semantic vector lifecycle queries entirely driven within Code Mode orchestrations without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 19 Error Handling & Security bounds, confirming input validation, blocked patterns, runtime error handling, and nulled globals without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 28 Team Admin & Collaboration bounds, including team operations (`teamUpdateEntry`, `teamDeleteEntry`, `teamMergeTags`), integrated analytics (`teamGetStatistics`), linking mechanisms (`teamLinkEntries`, `teamVisualizeRelationships`), localized IO handling (`teamExportEntries`, `teamExportMarkdown`, `teamImportMarkdown`), and independent administrative safeguards (`teamBackup`, `teamListBackups`, `teamGetCollaborationMatrix`), validating 100% boundary mapping into Code Mode execution targets without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 28.1-28.3 Team CRUD & Search bounds, covering `teamCreateEntry`, `teamGetRecent`, `teamSearch`, `teamGetEntryById`, `teamListTags`, and `teamSearchByDateRange`, including their respective structural schemas, required date formats, and error constraint paths without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 28.12-28.15 Team Flag Tools bounds, verifying `passTeamFlag` and `resolveTeamFlag` vocabulary execution, resolution lifecycles, structured error returns, search, and idempotency, after resolving the `autoContext` typing defect in the SQLite adapter.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 28.9-28.10 Team Vector, Insights & Cross-Tool Errors bounds, verifying `teamRebuildVectorIndex`, `teamGetCrossProjectInsights`, vector semantic search, and structured error boundary consistency for all cross-tool 404/invalid constraint paths without remediation.
- [x] **Code Mode Validation**: Certified 100% test-pass rate across all Phase 22 Multi-Step Workflows bounds, validating chained API calls, data transformation, conditional branching, and create/read/search round-trips without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 2 Core CRUD bounds (`create_entry_minimal`, `create_entry`, `get_entry_by_id`, `update_entry`, `delete_entry`, `get_recent_entries`), verifying metadata parsing (workflow, PR, project owner), validation schema adherence, relationships handling, soft/permanent deletes, and cross-state constraints without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 1 Core Infrastructure bounds, verifying server health, briefing resource assembly, protocol validation (tool annotations and instruction levels), and GitHub status endpoints without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 4 Relationships & Visualization bounds (`link_entries`, `visualize_relationships`), confirming all five relationship types (`references`, `implements`, `blocked_by`, `resolved`, `caused`), structural schemas, duplicate detection, graph formatting, and harmonized causal arrows without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 6 Automated Scheduler bounds, verifying accurate periodic job invocation via SSE/HTTP transport logic, concurrent health updating (`runCount`, `nextRun`, `lastResult`), multi-transport safety locks (deactivation under standard stdio mode), and independent isolation without cross-job pollution or regressions.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 3 Search & Semantics bounds, including FTS5 behaviors (phrase/prefix/boolean/filters), LIKE fallbacks, hybrid auto-mode heuristics (RRF), cross-database searching, date range filtering, and importance bounds sorting without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 3.2-3.3 Semantic Search & Analytics bounds (`semantic_search`, `get_vector_index_stats`, `rebuild_vector_index`, `get_statistics`, `get_cross_project_insights`, `add_to_vector_index`), verifying vector index updates, custom similarity thresholds, exact ID bypassing, multi-metric analytics groupings, dynamic project breakdown bounds, and structured zero/null propagation schemas without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 11 Error Handling & Structured Responses, verifying standard MCP prompt exception propagation (`-32602`), Zod schema type-boundary coercion routing (`isError: true` vs SDK JSON-RPC `-32602`), empty parameter bypass resolution, and structured `{ success: false }` outputs mapping rigidly via `.partial().passthrough()` tooling handlers without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across all Phase 12 Data Integrity & Boundary Check bounds, verifying complete Unicode, multiline, and HTML-like data preservation, relationship integrity matching, strict limit constraint mappings, partial update object retention, tag duplication deduplication, and precise Search Isolation of soft-deleted entries without data leakage or remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 13 Kanban Lifecycle tools (`get_kanban_board`, `add_kanban_item`, `move_kanban_item`, `delete_kanban_item`), verifying correct layout topological summaries (`summary_only: true`), seamless PR addition mappings, dynamic column transfer states ("In progress"), and safe project untethering without destructive issue side-effects without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 16 Payload Optimization bounds, verifying Kanban explicit throttles (`summary_only`, `item_limit`), dynamic GitHub Body Truncation strategies (`truncate_body`), global pagination threshold enforcement (`MAX_QUERY_LIMIT: 500`), and active Code Mode JSON bounds limits (`100KB` structured guidance alerts) accurately without returning raw exceptions or requiring remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 0 Output Schema Validation bounds, confirming that all 60 tools correctly utilize `structuredContent` formats routing structured JSON representations (including structured formatting of fallback error messages via the error wrapper) natively over generic raw text, fully mitigating client parsing crash risks without remediation.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 10 Team Collaboration and Hush Protocol bounds (24 tools + 4 resources), verifying team entry lifecycle, cross-project vector metrics, collaboration matrix data generation, markdown export pipelines, target user @ stripping, idempotent flag resolution states, and holistic tracking completely via direct MCP calls without remediation. Evaluated at 33,427 total output tokens successfully.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 5.1 Admin & Vector Tool Re-Test bounds, strictly verifying Zod Empty Param (`{}`), Type Mismatches, Vector Index fallback handling, and structured Domain Errors for `update_entry`, `delete_entry`, `merge_tags`, `add_to_vector_index`, and `rebuild_vector_index` natively without returning raw exceptions or crashes. Evaluated at 2,135 total output tokens successfully.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 5.2 Backup & Export Tool Re-Test bounds, strictly verifying Zod Schema coercion constraints, Type Mismatches, Path Traversal security perimeters, filter combinations (`export_entries`), and structural boundary limits for `backup_journal`, `restore_backup`, `export_entries`, and `cleanup_backups` natively without returning raw exceptions or crashes. Evaluated at 222 total output tokens successfully.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 5.3 Core Tool Group bounds, strictly verifying Zod Schema validation boundaries (`create_entry`, `create_entry_minimal`), domain constraints (`get_entry_by_id`), dynamic range limit enforcement (`get_recent_entries`), and cross-database analytics schemas (`get_statistics`) natively without returning raw exceptions or routing crashes. Evaluated at 3,708 total test session tokens successfully.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 5.4 GitHub Tool Group bounds, strictly verifying Zod Empty Param (`{}`), domain constraints (404 Not Found handling), Issue/PR mapping boundaries, Kanban mutation bounds, and GitHub insights partial failure paths natively without returning raw exceptions or crashes. OutputSchemas are not broken by enriched error states. Evaluated at 1,050 total test session tokens successfully.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 5.5 Search Tool Group bounds, strictly verifying Zod Empty Param (`{}`), Type Mismatches, Domain boundary checks (inverted dates), Soft Delete spatial isolation, and limit enforcement for `search_entries`, `search_by_date_range`, and `semantic_search` natively without returning raw exceptions or crashes. Evaluated at 13,676 total output tokens successfully.
- [x] **Direct Tool Validation**: Certified 100% test-pass rate across Phase 5.6 Team Tool Group bounds, strictly verifying Zod Empty Param (`{}`), Type Mismatches, Domain boundary checks (Unavailable Vector DB gracefully loading, Non-Flag resolution validation, and Flag idempotency checks), and structured Error mapping for all Team collaboration tools natively without returning raw exceptions or crashes. Evaluated at 16,000 total output tokens successfully.
